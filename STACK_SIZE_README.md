# SimpleStackSize - Настройка размеров стаков

## Описание
Плагин для настройки максимального размера стаков любых предметов в Rust. Теперь с веб-интерфейсом!

## Возможности
- ⚙️ Настройка размера стака для любого предмета
- 🌐 Удобный веб-интерфейс для управления
- 📊 Статистика по изменённым предметам
- 🔍 Поиск и фильтрация по категориям
- 💾 Экспорт/импорт настроек
- 🔄 Горячая перезагрузка без перезапуска сервера

## Установка

### 1. Установка плагина
1. Скопируйте файл `SimpleStackSize.cs` в папку `oxide/plugins/`
2. Плагин автоматически загрузится

### 2. Настройка через веб-интерфейс

#### Запуск веб-интерфейса:
```bash
npm install
node server.js
```

Откройте в браузере: `http://localhost:3000/stacks`

#### Возможности веб-интерфейса:
- Просмотр всех предметов с иконками
- Изменение размера стака для любого предмета
- Фильтрация по категориям (Оружие, Ресурсы, Броня и т.д.)
- Поиск по названию или shortname
- Просмотр только изменённых предметов
- Экспорт настроек в JSON
- Импорт настроек из JSON
- Сброс отдельных предметов или всех настроек

### 3. Применение настроек

После настройки через веб-интерфейс:

1. Файл `SimpleStackSize.json` будет создан автоматически
2. Скопируйте его в папку `oxide/data/` на сервере
3. Перезагрузите плагин командой: `stacksize.reload`

## Использование

### Веб-интерфейс

1. **Изменить размер стака:**
   - Найдите нужный предмет
   - Введите новое значение в поле "Макс"
   - Изменения сохраняются автоматически

2. **Сбросить предмет:**
   - Нажмите кнопку ❌ рядом с полем ввода
   - Или очистите поле и нажмите Enter

3. **Фильтрация:**
   - Используйте кнопки категорий для фильтрации
   - Кнопка "Изменённые" показывает только настроенные предметы
   - Поиск работает по названию и shortname

4. **Экспорт/Импорт:**
   - Кнопка "Экспорт" - скачать текущие настройки
   - Кнопка "Импорт" - загрузить настройки из файла
   - Кнопка "Очистить всё" - сбросить все настройки

### Консольные команды

```
stacksize.reload - Перезагрузить настройки из файла
stacksize.list   - Показать список всех настроенных стаков
```

## Формат файла SimpleStackSize.json

```json
{
  "scrap": 10000,
  "wood": 50000,
  "stones": 50000,
  "metal.fragments": 10000,
  "sulfur": 10000,
  "cloth": 5000,
  "ammo.rifle": 1000,
  "explosive.timed": 10
}
```

Где:
- Ключ - shortname предмета (например, "scrap", "wood")
- Значение - максимальный размер стака

## Примеры настроек

### Увеличенные ресурсы:
```json
{
  "wood": 100000,
  "stones": 100000,
  "metal.fragments": 50000,
  "metal.ore": 50000,
  "sulfur": 50000,
  "sulfur.ore": 50000,
  "cloth": 10000,
  "leather": 10000,
  "scrap": 50000
}
```

### Увеличенные патроны:
```json
{
  "ammo.rifle": 2000,
  "ammo.pistol": 2000,
  "ammo.shotgun": 2000,
  "arrow.wooden": 500,
  "arrow.hv": 500
}
```

### Уменьшенная взрывчатка (для баланса):
```json
{
  "explosive.timed": 5,
  "explosive.satchel": 10,
  "ammo.rocket.basic": 10,
  "grenade.f1": 20
}
```

## Обратная совместимость

Плагин поддерживает старый формат конфига (oxide/config/SimpleStackSize.json).
Если JSON файл не найден, настройки будут загружены из конфига.

Рекомендуется использовать новый формат через веб-интерфейс.

## Особенности

- Если предмет не указан в настройках, используется стандартный размер стака из Rust
- Изменения применяются сразу после перезагрузки плагина
- Не требует перезапуска сервера
- Работает с любыми предметами, включие модифицированные

## Поддержка

При возникновении проблем:
1. Проверьте правильность shortname предметов
2. Убедитесь, что JSON файл валидный
3. Проверьте логи сервера на ошибки
4. Используйте команду `stacksize.list` для проверки загруженных настроек

## Версия
2.0.0 - Добавлен веб-интерфейс и поддержка JSON файлов
